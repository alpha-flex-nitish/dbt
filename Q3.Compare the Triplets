#include<iostream>
#include<cmath>
#include <bits/stdc++.h>
using namespace std;

int main(){
        int i, a[i], b[i], n=3, aP=0, bP=0;

        for(i=0;i<n;i++)
        {
            cin >> a[i];
        }
        for(i=0;i<n;i++)
        {
            cin >> b[i];
        }
        for(i=0;i<n;i++)
            {
                if(a[i]<b[i])
                    {
                    bP++;
                    }
                else if(a[i]>b[i])
                    {
                    aP++;
                    }

            }
        cout << aP << " " << bP;
return 0;


}
